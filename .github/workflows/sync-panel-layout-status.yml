name: Synchronize panel layout status

on:
  push:
    branches:
      - agent/fix-compact-lyrics

permissions:
  contents: write

jobs:
  update-status:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/fix-compact-lyrics
          fetch-depth: 0

      - uses: oven-sh/setup-bun@v2

      - name